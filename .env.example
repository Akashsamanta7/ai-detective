# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# MULTI-KEY ROTATION (Optional)
# If you have multiple accounts, you can add more keys to bypass free tier rate limits.
# IMPORTANT: Additional keys MUST be prefixed with VITE_ to be visible to the browser.
VITE_GEMINI_API_KEY_1="SECOND_API_KEY"
VITE_GEMINI_API_KEY_2="THIRD_API_KEY"
# ... up to VITE_GEMINI_API_KEY_20

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# MONGODB_URI: Required for cloud persistence (e.g., MongoDB Atlas)
MONGODB_URI="mongodb+srv://<username>:<password>@cluster.mongodb.net/ai-detective"
